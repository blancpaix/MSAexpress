[Initialize]
in CMD:
  consul agent -dev

in project directory:
  npm run pay
  npm run auth

run Redis-server.exe in local;


[MSA Project]
api 서비스 별 Micro service 실행

Single Endpoint (reverse proxyy)
  -Custom Loadbalancer (node.js base loadBalancer)

  -Custom
    /auth
    /payment
    /chat

Session
  storing in in-memory DB (Redis)
    
    
Server List
  proxyLoadBalancer,
  auth-service server,
  pay-service -server,
  chat-service -server,

Mesh Service tools
  Consul

DB
  RDB
    auth-msa
    pay-msa
    chat-msa
  IN_MEMORY DB
    Redis for session

